name: Docker Image Build

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: cd docker | build_base_docker_github.sh | build_docker_github.sh
